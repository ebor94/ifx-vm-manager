{
  "name": "backend",
  "version": "0.1.0",
  "private": true,
  "description": "Backend API — Express + better-sqlite3 + Socket.io",
  "main": "index.js",
  "scripts": {
    "dev": "nodemon index.js",
    "start": "node index.js",
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "migrate": "node src/db/migrate.js",
    "seed": "node src/db/seed.js",
    "lint": "echo \"(lint pendiente de configurar)\" && exit 0"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^11.5.0",
    "cookie-parser": "^1.4.6",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "jsonwebtoken": "^9.0.2",
    "socket.io": "^4.7.5"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "nodemon": "^3.1.4",
    "supertest": "^6.3.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
